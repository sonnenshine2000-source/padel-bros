import { supabase } from './supabase.js';
import { $, escapeHtml } from './utils.js';

export async function loadHistory(){
 const el=$('historyList');if(!el)return;
 const q=await supabase.from('match_days').select('id,match_date,schedule_generated_at,assignments(court,position,manually_changed,players:player_id(id,name,is_stammspieler)),match_results(court,set1_home,set1_away,set2_home,set2_away,set3_home,set3_away,team1_player1,team1_player2,team2_player1,team2_player2)').order('match_date',{ascending:false}).limit(100);
 if(q.error){el.innerHTML=`<div class="status err">${escapeHtml(q.error.message)}</div>`;return;}
 const days=(q.data||[]).filter(d=>(d.match_results||[]).length>0);
 if(!days.length){el.innerHTML='<div class="sub">Noch keine gespielten Spiele in der Historie.</div>';return;}
 const score=r=>r?`<span class="history-score">${r.set1_home??'–'}:${r.set1_away??'–'} · ${r.set2_home??'–'}:${r.set2_away??'–'}${r.set3_home!=null||r.set3_away!=null?` · ${r.set3_home??'–'}:${r.set3_away??'–'}`:''}</span>`:'<span class="history-score muted">Kein Ergebnis eingetragen</span>';
 const winnerIds=r=>{if(!r)return new Set();let home=0,away=0;for(const n of [1,2,3]){const h=r[`set${n}_home`],a=r[`set${n}_away`];if(h==null||a==null)continue;if(Number(h)>Number(a))home++;else if(Number(a)>Number(h))away++;}if(home===away||Math.max(home,away)<2)return new Set();return new Set(home>away?[r.team1_player1,r.team1_player2].filter(Boolean):[r.team2_player1,r.team2_player2].filter(Boolean));};
 el.innerHTML=days.map(d=>{const by={court5:[],court1:[]};(d.assignments||[]).forEach(a=>{if(by[a.court])by[a.court].push(a)});const result=c=>(d.match_results||[]).find(r=>r.court===c);const fmt=(arr,c)=>{const r=result(c),wins=winnerIds(r);return arr.sort((a,b)=>a.position-b.position).map(a=>{const winner=wins.has(a.players?.id);return `<div class="history-player"${winner?' style="background:#fbf0f2;border:1px solid #e7b3bb;border-radius:9px;padding:7px 8px;margin:3px 0;font-weight:950"':''}><span>${winner?'🏆 ':''}${escapeHtml(a.players?.name||'Spieler')}</span>${a.players?.is_stammspieler?'<span class="badge">⭐ Stammspieler</span>':'<span class="badge">Ersatz</span>'}${a.manually_changed?'<span class="badge">✋ geändert</span>':''}</div>`}).join('');};return `<div class="history-day"><div class="history-date">${escapeHtml(d.match_date||'Datum unbekannt')}</div><div class="history-courts"><div><b>Court 5 · 18:30</b>${score(result('court5'))}${fmt(by.court5,'court5')}</div><div><b>Court 1 · 19:00</b>${score(result('court1'))}${fmt(by.court1,'court1')}</div></div></div>`}).join('');
}
